server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848  #服务注册中心地址
      config:
        server-addr: http://localhost:8848  #服务配置中心地址
        file-extension: yaml  #指定yaml配置的文件
#        group: DEV_GROUP
        namespace: 245611df-96d1-43f4-91c2-b26ba14f57ad

# nacos-config-client-dev.yaml